// Event management — /admin/events
// Protected: admin role
// Fetches: GET /api/v1/events
// Create event form: title, type, description, starts_at, ends_at, location_url
// Calls POST /api/v1/events

export { default } from "@/components/shared/ComingSoon"
