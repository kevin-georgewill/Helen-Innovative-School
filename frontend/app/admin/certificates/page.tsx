// Certificate management — /admin/certificates
// Protected: admin role
// Lists pending and issued certificates
// Approve action calls POST /api/v1/admin/certificates/:id/approve
// which triggers PDF generation and issuance

export { default } from "@/components/shared/ComingSoon"
