// Student management — /admin/students
// Protected: admin role
// Fetches: GET /api/v1/admin/students
// Searchable table: name, email, enrolled courses, join date, status
// Suspend action calls PATCH /api/v1/admin/students/:id/suspend

export { default } from "@/components/shared/ComingSoon"
