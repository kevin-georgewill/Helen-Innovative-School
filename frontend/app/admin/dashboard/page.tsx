// Admin dashboard — /admin/dashboard
// Protected: admin role
// Fetches: GET /api/v1/admin/stats
// Stat cards: total students, total courses, total revenue, total enrollments
// Recent enrollment and certificate activity tables

export { default } from "@/components/shared/ComingSoon"
