// Blog management — /admin/blog
// Protected: admin role
// Fetches: GET /api/v1/blog (all posts including drafts)
// Create / edit posts, toggle is_published
// Calls POST /api/v1/blog and PATCH /api/v1/blog/:id

export { default } from "@/components/shared/ComingSoon"
