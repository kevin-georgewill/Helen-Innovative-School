// Course management — /admin/courses
// Protected: admin role
// Fetches: GET /api/v1/courses (all, including unpublished)
// Table: title, faculty, instructor, enrollments count, published status
// Archive action calls DELETE /api/v1/courses/:id

export { default } from "@/components/shared/ComingSoon"
