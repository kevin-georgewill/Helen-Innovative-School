// Assessment manager — /instructor/assessments
// Protected: instructor role
// Create quiz: add questions with options, mark correct answer, set pass score
// Calls POST /api/v1/assessments
// View results table: student, score, passed, submitted_at
// Fetches: GET /api/v1/assessments/:id/results

export { default } from "@/components/shared/ComingSoon"
