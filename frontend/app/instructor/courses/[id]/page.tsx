// Course editor — /instructor/courses/[id]
// Protected: instructor role
// Fetches: GET /api/v1/courses/:slug
// Add / reorder modules, add lessons with file upload
// Calls POST /api/v1/courses/:id/modules and POST /api/v1/modules/:id/lessons

export { default } from "@/components/shared/ComingSoon"
