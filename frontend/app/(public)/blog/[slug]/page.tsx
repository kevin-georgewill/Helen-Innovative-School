// Blog post page — /blog/[slug]
// Fetches: GET /api/v1/blog/:slug
// Renders full article body (markdown or rich text), author card, related posts

export { default } from "@/components/shared/ComingSoon"
