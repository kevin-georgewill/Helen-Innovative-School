// Instructor profile page — /instructors/[id]
// Fetches: GET /api/v1/auth/me (instructor profile) + their courses
// Shows avatar, bio, specialization, list of courses taught

export { default } from "@/components/shared/ComingSoon"
