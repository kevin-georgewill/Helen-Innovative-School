// Single faculty page — /faculties/[slug]
// Fetches: GET /api/v1/faculties/:slug (includes nested courses)
// Shows faculty description + grid of its CourseCards

export { default } from "@/components/shared/ComingSoon"
