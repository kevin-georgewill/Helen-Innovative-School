// list   — select published blog posts ordered by published_at desc, join author name
// getOne — select post by slug, join author profile
// create — insert blog_post row with author_id from req.user.id
// update — update blog_post by id (toggle is_published, edit body, etc.)
