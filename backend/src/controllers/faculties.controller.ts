// list    — select all faculties ordered by name
// getOne  — select faculty by slug, include nested courses
// create  — insert new faculty row
// update  — update faculty by id
// remove  — delete faculty by id
