// generateCertificate({ studentName, courseName, certificateNo, verifyUrl })
// builds an A4 landscape PDF using pdf-lib, embeds a QR code pointing to verifyUrl,
// returns the PDF as a Uint8Array for upload to Supabase Storage
