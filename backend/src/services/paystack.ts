// initializeTransaction(email, amount, metadata) — creates a Paystack payment session,
// returns an authorization_url to redirect the student to

// verifyTransaction(reference) — confirms a transaction status after payment,
// called by the webhook handler to activate enrollment
