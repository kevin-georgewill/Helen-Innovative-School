// stripe — configured Stripe instance

// createPaymentIntent(amount, currency, metadata) — creates a PaymentIntent for
// the frontend to confirm using the Stripe.js client

// constructWebhookEvent(payload, sig) — verifies and parses an incoming Stripe webhook
